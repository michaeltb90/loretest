<%= form_with(model: race, local: true) do |form| %>
  <% if race.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(race.errors.count, "error") %> prohibited this race from being saved:</h2>

      <ul>
      <% race.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :race_name %>
  </div>

  <div class="form-group">
  <%= form.select :sentience_id, @sentiences.collect { |c| [ c.name, c.id ]}, {include_blank: "Please select a sentience" } %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :race_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
